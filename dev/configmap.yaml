apiVersion: v1
kind: ConfigMap
metadata:
  name: my-app-service-configmap
  namespace: myapp
  labels:
    app: my-app-service-configmap
    type: configmap
data:
  my-app-service-configmap: |
    APP_NAME: my-app-service
    APP_PORT: 80
